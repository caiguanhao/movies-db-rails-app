<%= bootstrap_form_for @timetable, url: movie_timetables_path(@movie), layout: :horizontal do |f| %>
  <%= f.date_select :date %>
  <%= f.time_select :time %>
  <%= f.select :cinema_id, Cinema.all.collect { |c| [ c.name, c.id ] } %>
  <%= f.submit %>
<% end %>
