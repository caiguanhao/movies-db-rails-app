<h2>正在热映 0 部</h2>
<div class="row">
<% @movies.each do |movie| %>
  <div class="col-sm-3">
    <%= link_to movie_timetables_path(movie) do %>
      <img src="">
      <%= movie.name %>
    <% end %>
  </div>
<% end %>
